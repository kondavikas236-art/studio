# Kidsyee - The Smart Screen Time Guardian

This is a NextJS project built with Firebase Studio.

## Deployment Note
If you see a "Site can't be reached" (DNS) error immediately after creation, please wait 5-10 minutes for the global DNS records to propagate.

## Getting Started
To get started, take a look at `src/app/page.tsx`.
